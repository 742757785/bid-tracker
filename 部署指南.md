# 部署指南 - 使用 GitHub Pages + Actions

## 步骤一：创建 GitHub 仓库

1. 登录 GitHub：https://github.com
2. 点击右上角 "+" → "New repository"
3. 仓库名填写：`bid-tracker`
4. 选择 "Public"
5. 点击 "Create repository"

## 步骤二：上传文件

将 `BidTrackerWeb` 文件夹中的所有文件上传到仓库：

```
bid-tracker/
├── .github/workflows/fetch-data.yml
├── fetch-data.js
├── index.html
├── data.json
└── package.json
```

## 步骤三：启用 GitHub Pages

1. 进入仓库 → Settings → Pages
2. Source 选择 "Deploy from a branch"
3. Branch 选择 "main"，文件夹选择 "/ (root)"
4. 点击 Save
5. 等待几分钟，会生成网址：`https://你的用户名.github.io/bid-tracker/`

## 步骤四：运行 GitHub Actions

1. 进入仓库 → Actions
2. 如果提示启用，点击 "I understand my workflows, go ahead and enable them"
3. 点击 "抓取招标信息" → "Run workflow" → "Run workflow"
4. 等待运行完成（约1-2分钟）
5. 运行成功后，data.json 会自动更新

## 步骤五：访问应用

打开网址：`https://你的用户名.github.io/bid-tracker/`

## 自动更新

GitHub Actions 会每天自动运行两次：
- 早上 8:00
- 下午 18:00

也可以手动触发：Actions → 抓取招标信息 → Run workflow

## 添加到手机

### Android
1. 用 Chrome 打开网址
2. 点击菜单 (三个点) → 添加到主屏幕

### iPhone
1. 用 Safari 打开网址
2. 点击分享按钮 → 添加到主屏幕

---

## 常见问题

**Q: 为什么没有数据？**
A: 需要先运行 GitHub Actions 抓取数据。

**Q: 数据多久更新一次？**
A: 每天两次（8:00 和 18:00），也可手动触发。

**Q: 可以添加更多数据源吗？**
A: 可以，修改 `fetch-data.js` 文件添加新的解析函数。